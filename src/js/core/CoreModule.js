/**
 * Core/Core.Module
 * ----------------------------------------------------------------------
 * Módulo "core" da aplicação, declara filtros e serviços, ocasionalmente 
 * diretivas que não necessitam de templates, apenas processando dados.
 * 
 * @type    {angular.Module}
 * @author  Fabio Y. Goto <lab@yuiti.com.br>
 * @since   0.0.1
 */
const Core = angular.module("Core", []);
