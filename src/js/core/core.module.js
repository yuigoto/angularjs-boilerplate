/**
 * Core/Core.Module
 * ----------------------------------------------------------------------
 * Core module.
 * 
 * Inclui filtros, factories e outros globais que, normalmente, não possuem 
 * templates HTML.
 * 
 * @type    {angular.Module}
 * @since   0.0.1
 */
const core = angular.module("core", []);
