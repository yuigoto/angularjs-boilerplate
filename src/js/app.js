/**
 * App
 * ----------------------------------------------------------------------
 * It's an APP! An App!
 * 
 * @type    {angular.Module}
 * @since   0.0.1
 */
const app = angular.module("app", [
  "ngRoute",
  "base", 
  "core"
]);
