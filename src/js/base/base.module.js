/**
 * Base/Base.Module
 * ----------------------------------------------------------------------
 * Base module.
 * 
 * Módulo principal da aplicação, contém módulos básicos.
 * 
 * @type    {angular.Module}
 * @since   0.0.1
 */
const base = angular.module("base", []);
